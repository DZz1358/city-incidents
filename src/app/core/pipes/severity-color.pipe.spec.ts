/* tslint:disable:no-unused-variable */

import { SeverityColorPipe } from './severity-color.pipe';

describe('Pipe: SeverityColore', () => {
  it('create an instance', () => {
    let pipe = new SeverityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
